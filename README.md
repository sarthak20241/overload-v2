
  # Redesign Gym Workout Tracker UI

  This is a code bundle for Redesign Gym Workout Tracker UI. The original project is available at https://www.figma.com/design/zVzWbsa1ZinDUan71mDSyR/Redesign-Gym-Workout-Tracker-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  