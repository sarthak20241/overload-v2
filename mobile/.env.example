# Supabase (use these exact names; copy to .env and fill in your values)
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Optional: for seeding dummy data (npm run seed). Prefer passing via env when running.
# SEED_USER_EMAIL=your@email.com
# SEED_USER_PASSWORD=your_password
